import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Debugging ke liye React Strict Mode enable karein
  reactStrictMode: true,

  // Yahan apne project ke additional configurations add karen
};

export default nextConfig;
